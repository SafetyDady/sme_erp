# JWT Configuration
JWT_SECRET_KEY=change_this_super_long_random_secret_key_in_production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=14

# Database Configuration
DATABASE_URL=sqlite:///./sme_erp.db

# Read Replica Configuration (Phase 9)
READ_REPLICA_ENABLED=false
READ_REPLICA_DATABASE_URL=sqlite:///./sme_erp_replica.db
READ_REPLICA_FALLBACK=true

# CORS Configuration
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]

# Environment
ENVIRONMENT=development
DEBUG=true

# Audit Configuration
AUDIT_ENABLED=true

# Export Configuration
EXPORTS_DIR=./exports
EXPORT_CLEANUP_DAYS=7